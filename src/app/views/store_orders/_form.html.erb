<%= form_with(model: store_order, local: true) do |form| %>
  <% if store_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_order.errors.count, "error") %> prohibited this store_order from being saved:</h2>

      <ul>
      <% store_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
