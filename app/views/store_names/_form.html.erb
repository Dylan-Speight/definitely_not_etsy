<%= form_with(model: store_name, local: true) do |form| %>
  <% if store_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_name.errors.count, "error") %> prohibited this store_name from being saved:</h2>

      <ul>
      <% store_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
